{
  "name": "soundchain-contracts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.mjs --network polygonAmoy",
    "deploy:mainnet": "hardhat run scripts/deploy-mainnet.mjs --network polygonMainnet --config hardhat.config.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3",
    "ethers": "^6.14.0",
    "hardhat": "^2.22.0"
  }
}
